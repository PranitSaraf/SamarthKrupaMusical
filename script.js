/*__DATA_START__*/
window.SITE_DATA = {
  "settings": {
    "storeName": "Shri Samarth Krupa Musical & Electronics",
    "shortName": "Shri Samarth Krupa Musical",
    "whatsappNumber": "918308651469",
    "storeAddress": "Main Road, Raver, District Jalgaon (MH), Pincode 425508",
    "phoneOne": "9359776263",
    "phoneTwo": "9405724793",
    "instagramOne": "jayesh_sugandhiwale",
    "instagramTwo": "skmusical2021",
    "amazonLink": "",
    "flipkartLink": "",
    "meeshoLink": "",
    "storeTagline": "All kinds of Musical Instruments • Wholesaler & Retailer • Sales and Service"
  },
  "categories": [
    {
      "id": 1,
      "name": "Harmonium",
      "icon": "🎹",
      "copy": "Traditional reed instruments for bhajan and classical music."
    },
    {
      "id": 2,
      "name": "Tabla",
      "icon": "🥁",
      "copy": "Classical hand percussion for practice, stage, and teaching."
    },
    {
      "id": 3,
      "name": "Keyboard",
      "icon": "⌨️",
      "copy": "Portable and performance-style keyboards for all levels."
    },
    {
      "id": 4,
      "name": "Guitar",
      "icon": "🎸",
      "copy": "Acoustic guitars for learners, hobby players, and performers."
    },
    {
      "id": 5,
      "name": "Dholak",
      "icon": "🪘",
      "copy": "Rhythmic folk instruments for festive and devotional settings."
    },
    {
      "id": 6,
      "name": "Musical Accessories",
      "icon": "🎤",
      "copy": "Useful add-ons like microphones, stands, and support gear."
    }
  ],
  "products": [
    {
      "id": 1,
      "name": "Swaranjali Classic Harmonium",
      "category": "Harmonium",
      "price": 18999,
      "oldPrice": 21999,
      "badge": "Bestseller",
      "description": "Traditional harmonium with warm tonal character for devotional and classical use.",
      "longDescription": "A premium-looking harmonium suited for bhajans, home practice, and stage performance. Designed for a polished product showcase with a rich wooden look and elegant front profile.",
      "highlights": [
        "Smooth key response",
        "Portable folding body",
        "Ideal for devotional music"
      ],
      "image": "https://images.unsplash.com/photo-1511379938547-c1f69419868d?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1511379938547-c1f69419868d?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": true,
      "visible": true,
      "inStock": true,
      "order": 1
    },
    {
      "id": 2,
      "name": "Mehfil Deluxe Harmonium",
      "category": "Harmonium",
      "price": 22999,
      "oldPrice": 25999,
      "badge": "Premium",
      "description": "Decorative harmonium styling with a premium visual presentation.",
      "longDescription": "A visually rich harmonium option for customers looking for an instrument with elegant decorative appeal. Great for showroom display, gifts, and devotional performance setups.",
      "highlights": [
        "Premium visual appeal",
        "Elegant body design",
        "Showroom-ready presentation"
      ],
      "image": "https://images.unsplash.com/photo-1511379938547-c1f69419868d?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1511379938547-c1f69419868d?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 2
    },
    {
      "id": 3,
      "name": "Naad Sheesham Tabla Set",
      "category": "Tabla",
      "price": 15999,
      "oldPrice": 18499,
      "badge": "Popular",
      "description": "Classic tabla pair with clear dayan response and strong bayan depth.",
      "longDescription": "A refined tabla set suitable for learners and intermediate players who want a polished appearance and balanced tonal response for regular riyaz and live presentation.",
      "highlights": [
        "Balanced sound character",
        "Performance-ready look",
        "Traditional percussion appeal"
      ],
      "image": "https://images.unsplash.com/photo-1516280440614-37939bbacd81?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1516280440614-37939bbacd81?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": true,
      "visible": true,
      "inStock": true,
      "order": 3
    },
    {
      "id": 4,
      "name": "Riyaaz Copper Bayan Tabla",
      "category": "Tabla",
      "price": 23999,
      "oldPrice": 26999,
      "badge": "Artist Pick",
      "description": "Premium tabla styling with strong stage presence and handcrafted detail.",
      "longDescription": "Designed for players who want a more premium visual impression with dependable sound feel. A polished product for classical music lovers and serious performers.",
      "highlights": [
        "Stage-friendly look",
        "Premium percussion styling",
        "Suitable for advanced players"
      ],
      "image": "https://images.unsplash.com/photo-1516280440614-37939bbacd81?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1516280440614-37939bbacd81?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 4
    },
    {
      "id": 5,
      "name": "SwarKey 61 Student Keyboard",
      "category": "Keyboard",
      "price": 14499,
      "oldPrice": 16999,
      "badge": "New",
      "description": "Compact keyboard for music classes, home practice, and learner setups.",
      "longDescription": "A practical keyboard choice for students and hobby musicians. Its clean design makes it easy to present as a catalog item for educational and personal music use.",
      "highlights": [
        "Beginner friendly",
        "Clean modern design",
        "Great for home use"
      ],
      "image": "https://images.unsplash.com/photo-1507838153414-b4b713384a76?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1507838153414-b4b713384a76?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 5
    },
    {
      "id": 6,
      "name": "StageTone Performance Keyboard",
      "category": "Keyboard",
      "price": 28999,
      "oldPrice": 31999,
      "badge": "Featured",
      "description": "Performance-style keyboard with a premium stage-ready presentation.",
      "longDescription": "A more advanced keyboard option for customers who want a product that looks substantial, premium, and suitable for studio or live-performance environments.",
      "highlights": [
        "Stage-ready styling",
        "Premium presentation",
        "Ideal for serious users"
      ],
      "image": "https://images.unsplash.com/photo-1507838153414-b4b713384a76?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1507838153414-b4b713384a76?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 6
    },
    {
      "id": 7,
      "name": "Aarambh Acoustic Guitar",
      "category": "Guitar",
      "price": 8999,
      "oldPrice": 10499,
      "badge": "Starter Favorite",
      "description": "Warm-toned acoustic guitar for beginners, students, and home practice.",
      "longDescription": "A comfortable entry-level acoustic guitar with a clean natural finish. Perfect for music classes, gifting, and customers starting their playing journey.",
      "highlights": [
        "Comfortable body shape",
        "Natural acoustic look",
        "Beginner friendly"
      ],
      "image": "https://images.unsplash.com/photo-1510915361894-db8b60106cb1?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1510915361894-db8b60106cb1?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": true,
      "visible": true,
      "inStock": true,
      "order": 7
    },
    {
      "id": 8,
      "name": "SurTarang Cutaway Guitar",
      "category": "Guitar",
      "price": 13499,
      "oldPrice": 15499,
      "badge": "Modern",
      "description": "Modern acoustic guitar styling with a clean premium finish and broad appeal.",
      "longDescription": "A stylish guitar for catalog presentation, suitable for learners, casual performers, and customers who prefer a more premium visual finish in an acoustic instrument.",
      "highlights": [
        "Modern display appeal",
        "Versatile use case",
        "Premium finish look"
      ],
      "image": "https://images.unsplash.com/photo-1510915361894-db8b60106cb1?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1510915361894-db8b60106cb1?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 8
    },
    {
      "id": 9,
      "name": "Festival Dholak Classic",
      "category": "Dholak",
      "price": 7499,
      "oldPrice": 8699,
      "badge": "Value Pick",
      "description": "Traditional hand drum style for folk, bhajan, and festive performances.",
      "longDescription": "A compact dholak-style product with strong visual appeal for devotional gatherings, festive programs, and live folk music settings.",
      "highlights": [
        "Folk performance ready",
        "Portable body",
        "Great for festive use"
      ],
      "image": "https://images.unsplash.com/photo-1571330735066-03aaa9429d89?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1571330735066-03aaa9429d89?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 9
    },
    {
      "id": 10,
      "name": "Lehra Hand Drum Edition",
      "category": "Dholak",
      "price": 9999,
      "oldPrice": 11499,
      "badge": "Handcrafted",
      "description": "Handheld drum look with a strong performance vibe and traditional identity.",
      "longDescription": "A more premium dholak-style showcase item intended for performers and customers who want a compact rhythmic instrument with character and visual charm.",
      "highlights": [
        "Traditional rhythm look",
        "Performance-friendly feel",
        "Compact and striking"
      ],
      "image": "https://images.unsplash.com/photo-1571330735066-03aaa9429d89?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1571330735066-03aaa9429d89?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 10
    },
    {
      "id": 11,
      "name": "Stage Mic Stand Pro",
      "category": "Musical Accessories",
      "price": 2499,
      "oldPrice": 2999,
      "badge": "Essential",
      "description": "Elegant microphone stand setup for stage, events, and vocal practice spaces.",
      "longDescription": "A useful accessory item that rounds out your music catalog. Ideal for singers, public performances, rehearsal rooms, and event setups.",
      "highlights": [
        "Stage-ready design",
        "Useful accessory item",
        "Ideal for singers and events"
      ],
      "image": "https://images.unsplash.com/photo-1516280030429-27679b3dc9cf?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1516280030429-27679b3dc9cf?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 11
    },
    {
      "id": 12,
      "name": "Studio Vocal Microphone",
      "category": "Musical Accessories",
      "price": 3999,
      "oldPrice": 4999,
      "badge": "Trending",
      "description": "Studio-style microphone accessory for recording, vocals, and content creators.",
      "longDescription": "A polished audio accessory for users who want a professional-looking microphone for singing practice, podcasting, or studio display setups.",
      "highlights": [
        "Studio recording look",
        "Premium accessory option",
        "Good for creators and vocals"
      ],
      "image": "https://images.unsplash.com/photo-1516280030429-27679b3dc9cf?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1516280030429-27679b3dc9cf?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 12
    },
    {
      "id": 13,
      "name": "DJ",
      "category": "Musical Accessories",
      "price": 190999,
      "oldPrice": 194999,
      "badge": "Trending",
      "description": "Professional DJ setup accessory for live performance and studio use.",
      "longDescription": "A premium DJ-style music accessory suited for events, performances, and showroom display where a modern audio setup is needed.",
      "highlights": [
        "Premium setup",
        "Suitable for live events",
        "Professional presentation"
      ],
      "image": "https://images.unsplash.com/photo-1516280030429-27679b3dc9cf?auto=format&fit=crop&w=1200&q=80",
      "images": [
        "https://images.unsplash.com/photo-1516280030429-27679b3dc9cf?auto=format&fit=crop&w=1200&q=80"
      ],
      "featured": false,
      "visible": true,
      "inStock": true,
      "order": 13
    }
  ],
  "inquiries": []
};
/*__DATA_END__*/


(function () {
  const data = window.SITE_DATA || { settings: {}, categories: [], products: [], inquiries: [] };
  const ADMIN_USERNAME = 'Pranit@01';
  const ADMIN_PASSWORD = 'Pranit@01';
  function qs(selector, root = document) { return root.querySelector(selector); }
  function qsa(selector, root = document) { return Array.from(root.querySelectorAll(selector)); }
  function escapeRegex(text) { return String(text).replace(/[.*+?^${}()|[\\]\\]/g, '\\$&'); }
  function safeText(value) { return String(value ?? '').replace(/[&<>"']/g, m => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[m])); }
  function formatPrice(value) { return new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(Number(value || 0)); }
  function discountPercentage(price, oldPrice) { price = Number(price || 0); oldPrice = Number(oldPrice || 0); if (!oldPrice || oldPrice <= price) return 0; return Math.round(((oldPrice - price) / oldPrice) * 100); }
  function showToast(message) { const toast = qs('#toast'); if (!toast) return; toast.textContent = message; toast.classList.add('show'); clearTimeout(showToast._timer); showToast._timer = setTimeout(() => toast.classList.remove('show'), 2200); }
  function whatsappLink(product) { const number = (data.settings.whatsappNumber || '918308651469').replace(/\D/g, ''); const storeName = data.settings.shortName || data.settings.storeName || 'Shri Samarth Krupa Musical'; const message = `Hello ${storeName}, I am interested in "${product.name}" (${formatPrice(product.price)}). Please share more details.`; return `https://wa.me/${number}?text=${encodeURIComponent(message)}`; }
  function getVisibleProducts() { return (data.products || []).filter(product => product.visible !== false).slice().sort((a, b) => (Number(a.order || 9999) - Number(b.order || 9999)) || (a.id - b.id)); }
  function getFeaturedProducts() { const visible = getVisibleProducts(); const featured = visible.filter(product => product.featured); return (featured.length ? featured : visible).slice(0, 4); }
  function getCategoryCounts() { const counts = {}; getVisibleProducts().forEach(product => { counts[product.category] = (counts[product.category] || 0) + 1; }); return counts; }
  function maxProductPrice() { const prices = getVisibleProducts().map(product => Number(product.price || 0)); const max = prices.length ? Math.max(...prices) : 50000; return Math.max(50000, Math.ceil(max / 5000) * 5000); }
  function setTextAll(selector, value) { qsa(selector).forEach(el => { el.textContent = value || ''; }); }
  function updateNav() { const page = document.body.dataset.page; qsa('.nav-links a').forEach(link => { if (link.dataset.page === page) link.classList.add('active'); }); const toggle = qs('.nav-toggle'); const menu = qs('.nav-menu'); if (toggle && menu) toggle.addEventListener('click', () => menu.classList.toggle('open')); }
  function updateSettingsUI() {
    setTextAll('[data-store-name]', data.settings.shortName || data.settings.storeName || 'Shri Samarth Krupa Musical');
    setTextAll('[data-store-fullname]', data.settings.storeName || 'Shri Samarth Krupa Musical & Electronics');
    setTextAll('[data-address]', data.settings.storeAddress || '');
    setTextAll('[data-phone-one]', data.settings.phoneOne || '');
    setTextAll('[data-phone-two]', data.settings.phoneTwo || '');
    setTextAll('[data-tagline]', data.settings.storeTagline || '');
    qsa('[data-whatsapp-link]').forEach(el => { el.setAttribute('href', whatsappLink({ name: 'your products', price: 0 })); });
    const phoneOne = data.settings.phoneOne ? `tel:+91${String(data.settings.phoneOne).replace(/\D/g, '')}` : '#';
    const phoneTwo = data.settings.phoneTwo ? `tel:+91${String(data.settings.phoneTwo).replace(/\D/g, '')}` : '#';
    const phoneOneLink = qs('#phoneOneLink'); const phoneTwoLink = qs('#phoneTwoLink'); const phoneOneBtn = qs('#phoneOneBtn'); const phoneTwoBtn = qs('#phoneTwoBtn');
    if (phoneOneLink) phoneOneLink.href = phoneOne; if (phoneTwoLink) phoneTwoLink.href = phoneTwo; if (phoneOneBtn) phoneOneBtn.href = phoneOne; if (phoneTwoBtn) phoneTwoBtn.href = phoneTwo;
  }
  function categoryCard(category, count) { return `<a class="category-card" href="products.html?category=${encodeURIComponent(category.name)}"><div class="category-icon">${safeText(category.icon || '•')}</div><h3>${safeText(category.name)}</h3><p>${safeText(category.copy || '')}</p><div class="category-meta"><span>${count || 0} products</span><span>View catalog →</span></div></a>`; }
  function productCard(product) { const discount = discountPercentage(product.price, product.oldPrice); const stockChip = product.inStock === false ? '<span class="stock-chip out">Out of stock</span>' : '<span class="stock-chip">In stock</span>'; return `<article class="product-card"><div class="product-media"><img src="${safeText(product.image || '')}" alt="${safeText(product.name)}" loading="lazy"></div><div class="product-body"><div class="product-top"><span class="badge-soft">${safeText(product.badge || 'Product')}</span><span class="product-category">${safeText(product.category)}</span></div><h3 class="product-title">${safeText(product.name)}</h3><p class="product-desc">${safeText(product.description || '')}</p><div class="price-row"><span class="price">${formatPrice(product.price)}</span><span class="old-price">${formatPrice(product.oldPrice)}</span>${discount ? `<span class="discount">${discount}% off</span>` : ''}</div><div class="price-row" style="margin-top:0">${stockChip}</div><div class="card-actions"><a class="btn btn-soft" href="details.html?id=${product.id}">View details</a><a class="btn btn-primary" target="_blank" rel="noopener" href="${whatsappLink(product)}">WhatsApp inquiry</a></div></div></article>`; }
  function renderHeroQuickLinks() { const root = qs('#heroQuickLinks'); if (!root) return; root.innerHTML = (data.categories || []).slice(0, 6).map(category => `<a class="quick-link" href="products.html?category=${encodeURIComponent(category.name)}">${safeText(category.name)}</a>`).join(''); }
  function renderHome() { const featuredRoot = qs('#featuredProducts'); const categoriesRoot = qs('#homeCategories'); if (featuredRoot) featuredRoot.innerHTML = getFeaturedProducts().map(productCard).join(''); if (categoriesRoot) { const counts = getCategoryCounts(); categoriesRoot.innerHTML = (data.categories || []).map(category => categoryCard(category, counts[category.name] || 0)).join(''); } renderHeroQuickLinks(); }
  function renderCategories() { const root = qs('#categoryGrid'); if (!root) return; const counts = getCategoryCounts(); root.innerHTML = (data.categories || []).map(category => categoryCard(category, counts[category.name] || 0)).join(''); }
  function renderProducts() {
    const root = qs('#productGrid'); if (!root) return;
    const searchInput = qs('#searchInput'); const categorySelect = qs('#categorySelect'); const priceRange = qs('#priceRange'); const sortSelect = qs('#sortSelect'); const priceValue = qs('#priceValue'); const resultsText = qs('#resultsText');
    const params = new URLSearchParams(window.location.search); const initialCategory = params.get('category') || 'All'; const max = maxProductPrice();
    if (categorySelect) { categorySelect.innerHTML = '<option value="All">All</option>' + (data.categories || []).map(category => `<option value="${safeText(category.name)}">${safeText(category.name)}</option>`).join(''); categorySelect.value = initialCategory; }
    if (priceRange) { priceRange.max = String(max); priceRange.value = String(max); }
    function draw() {
      let list = getVisibleProducts(); const query = (searchInput?.value || '').trim().toLowerCase(); const category = categorySelect?.value || 'All'; const maxPrice = Number(priceRange?.value || max); const sort = sortSelect?.value || 'featured'; if (priceValue) priceValue.textContent = formatPrice(maxPrice);
      list = list.filter(product => { const blob = [product.name, product.category, product.description, product.longDescription].join(' ').toLowerCase(); return (!query || blob.includes(query)) && (category === 'All' || product.category === category) && Number(product.price || 0) <= maxPrice; });
      if (sort === 'priceLowHigh') list.sort((a, b) => Number(a.price) - Number(b.price)); else if (sort === 'priceHighLow') list.sort((a, b) => Number(b.price) - Number(a.price)); else if (sort === 'nameAZ') list.sort((a, b) => String(a.name).localeCompare(String(b.name))); else list.sort((a, b) => (a.featured === b.featured ? 0 : a.featured ? -1 : 1) || (Number(a.order || 9999) - Number(b.order || 9999)));
      if (resultsText) resultsText.textContent = `Showing ${list.length} product${list.length === 1 ? '' : 's'}`;
      root.innerHTML = list.length ? list.map(productCard).join('') : '<div class="empty-state"><h3>No products found</h3><p>Try changing search, category, or price filters.</p></div>';
    }
    [searchInput, categorySelect, priceRange, sortSelect].forEach(element => { if (!element) return; element.addEventListener('input', draw); element.addEventListener('change', draw); });
    const clearBtn = qs('#clearFiltersBtn'); if (clearBtn) clearBtn.addEventListener('click', () => { if (searchInput) searchInput.value = ''; if (categorySelect) categorySelect.value = 'All'; if (priceRange) priceRange.value = String(max); if (sortSelect) sortSelect.value = 'featured'; draw(); });
    draw();
  }
  function renderDetails() {
    const root = qs('#detailRoot'); if (!root) return; const visible = getVisibleProducts(); if (!visible.length) { root.innerHTML = '<div class="empty-state"><h3>No visible products</h3><p>Add or show products from admin first.</p></div>'; return; }
    const params = new URLSearchParams(window.location.search); const id = Number(params.get('id') || visible[0].id); const product = visible.find(item => item.id === id) || visible[0]; const images = (Array.isArray(product.images) && product.images.length ? product.images : [product.image]).filter(Boolean);
    root.innerHTML = `<div class="detail-grid"><div><div class="detail-image slider"><img id="detailMainImage" src="${safeText(images[0] || '')}" alt="${safeText(product.name)}">${images.length > 1 ? '<div class="slider-controls"><button class="slider-btn" type="button" id="sliderPrev">‹</button><button class="slider-btn" type="button" id="sliderNext">›</button></div>' : ''}<div class="slider-dots">${images.map((_, index) => `<button class="slider-dot ${index === 0 ? 'active' : ''}" type="button" data-slide="${index}"></button>`).join('')}</div></div></div><div><span class="badge-soft">${safeText(product.badge || 'Product')}</span><h1 class="detail-title">${safeText(product.name)}</h1><p class="muted">${safeText(product.category)}</p><p class="detail-copy">${safeText(product.longDescription || '')}</p><div class="price-row"><p class="detail-price">${formatPrice(product.price)}</p><span class="old-price">${formatPrice(product.oldPrice)}</span>${discountPercentage(product.price, product.oldPrice) ? `<span class="discount">Save ${discountPercentage(product.price, product.oldPrice)}%</span>` : ''}</div><div class="detail-highlights">${(product.highlights || []).map(item => `<div class="detail-feature">✓ ${safeText(item)}</div>`).join('')}</div><div class="inline-actions"><a class="btn btn-success" target="_blank" rel="noopener" href="${whatsappLink(product)}">WhatsApp inquiry</a><a class="btn btn-soft" href="products.html?category=${encodeURIComponent(product.category)}">More ${safeText(product.category)}</a></div></div></div>`;
    const relatedRoot = qs('#relatedProducts'); if (relatedRoot) relatedRoot.innerHTML = visible.filter(item => item.category === product.category && item.id !== product.id).slice(0, 4).map(productCard).join('');
    const mainImage = qs('#detailMainImage'); const dots = qsa('.slider-dot', root); const prevBtn = qs('#sliderPrev', root); const nextBtn = qs('#sliderNext', root); let current = 0; let timer;
    function show(index) { current = (index + images.length) % images.length; if (mainImage) mainImage.src = images[current]; dots.forEach((dot, i) => dot.classList.toggle('active', i === current)); }
    function startAuto() { if (images.length < 2) return; stopAuto(); timer = setInterval(() => show(current + 1), 3000); }
    function stopAuto() { if (timer) clearInterval(timer); }
    dots.forEach(dot => dot.addEventListener('click', () => { show(Number(dot.dataset.slide)); startAuto(); })); if (prevBtn) prevBtn.addEventListener('click', () => { show(current - 1); startAuto(); }); if (nextBtn) nextBtn.addEventListener('click', () => { show(current + 1); startAuto(); }); const slider = qs('.slider', root); if (slider) { slider.addEventListener('mouseenter', stopAuto); slider.addEventListener('mouseleave', startAuto); } startAuto();
  }
  function renderContact() {
    const instagramRoot = qs('#contactInstagramLinks'); const marketRoot = qs('#contactMarketLinks');
    if (instagramRoot) { const items = [data.settings.instagramOne, data.settings.instagramTwo].filter(Boolean); instagramRoot.innerHTML = items.length ? items.map(handle => `<a class="social-link" target="_blank" rel="noopener" href="https://instagram.com/${encodeURIComponent(handle)}"><span class="market-logo">📷</span><span>@${safeText(handle)}</span></a>`).join('') : '<span class="muted">No Instagram links added</span>'; }
    if (marketRoot) { const links = [{ key: 'amazonLink', label: 'Amazon', cls: 'amazon', logo: '🛒' }, { key: 'flipkartLink', label: 'Flipkart', cls: 'flipkart', logo: '🛍️' }, { key: 'meeshoLink', label: 'Meesho', cls: 'meesho', logo: '🏷️' }]; marketRoot.innerHTML = links.map(item => { const href = data.settings[item.key] || ''; const tag = href ? 'a' : 'span'; const attrs = href ? `href="${safeText(href)}" target="_blank" rel="noopener"` : ''; const disabledClass = href ? '' : ' disabled'; return `<${tag} class="market-badge ${item.cls}${disabledClass}" ${attrs}><span class="market-logo">${item.logo}</span><span>${item.label}</span></${tag}>`; }).join(''); }
  }
  function setStatus(message, isError) { const el = qs('#adminStatus'); if (!el) return; el.textContent = message || ''; el.classList.toggle('hidden', !message); el.classList.toggle('error', !!isError); }
  function downloadText(filename, text) { const blob = new Blob([text], { type: 'application/javascript' }); const link = document.createElement('a'); link.href = URL.createObjectURL(blob); link.download = filename; link.click(); setTimeout(() => URL.revokeObjectURL(link.href), 1500); }
  async function downloadScriptFile() { const currentScript = qs('script[src$="script.js"]'); const scriptUrl = currentScript ? currentScript.getAttribute('src') : 'script.js'; const response = await fetch(scriptUrl, { cache: 'no-store' }); if (!response.ok) throw new Error('Unable to fetch current script.js'); let source = await response.text(); const start = '/*__DATA_START__*/'; const end = '/*__DATA_END__*/'; const block = `${start}\nwindow.SITE_DATA = ${JSON.stringify(data, null, 2)};\n${end}`; const pattern = new RegExp(`${escapeRegex(start)}[\\s\\S]*?${escapeRegex(end)}`); if (pattern.test(source)) source = source.replace(pattern, block); else source = `${block}\n\n${source}`; downloadText('script.js', source); }
  function adminRenderStats() { const products = data.products || []; const visible = products.filter(item => item.visible !== false); const featured = visible.filter(item => item.featured); const set = (id, value) => { const el = qs(id); if (el) el.textContent = String(value); }; set('#statProducts', products.length); set('#statVisible', visible.length); set('#statFeatured', featured.length); set('#statCategories', (data.categories || []).length); }
  function adminPopulateCategoryOptions() { const select = qs('#adminCategory'); const filter = qs('#adminProductCategoryFilter'); const options = (data.categories || []).map(category => `<option value="${safeText(category.name)}">${safeText(category.name)}</option>`).join(''); if (select) select.innerHTML = options; if (filter) filter.innerHTML = '<option value="all">All categories</option>' + options; }
  function adminRenderProducts() { const root = qs('#adminProductsTable'); if (!root) return; const search = (qs('#adminProductSearch')?.value || '').trim().toLowerCase(); const category = qs('#adminProductCategoryFilter')?.value || 'all'; const list = (data.products || []).slice().sort((a, b) => (Number(a.order || 9999) - Number(b.order || 9999)) || (a.id - b.id)).filter(product => { const blob = [product.name, product.category, product.description, product.badge].join(' ').toLowerCase(); return (!search || blob.includes(search)) && (category === 'all' || product.category === category); }); root.innerHTML = list.length ? list.map(product => `<tr><td><img class="mini-img" src="${safeText(product.image || '')}" alt="${safeText(product.name)}"></td><td><strong>${safeText(product.name)}</strong><br><span class="muted">${safeText(product.category)}</span></td><td>${formatPrice(product.price)}</td><td><span class="tag ${product.inStock === false ? 'red' : 'green'}">${product.inStock === false ? 'Out of stock' : 'In stock'}</span><br><span class="tag ${product.visible === false ? 'red' : 'gray'}" style="margin-top:.35rem;">${product.visible === false ? 'Hidden' : 'Shown'}</span></td><td class="actions-row"><button type="button" class="btn-mini" data-edit-product="${product.id}">Edit</button><button type="button" class="btn-mini" data-toggle-visible="${product.id}">${product.visible === false ? 'Show' : 'Hide'}</button><button type="button" class="btn-mini" data-toggle-stock="${product.id}">${product.inStock === false ? 'Stock On' : 'Stock Off'}</button><button type="button" class="btn-mini" data-move-up="${product.id}">Up</button><button type="button" class="btn-mini" data-move-down="${product.id}">Down</button><button type="button" class="btn-mini" data-delete-product="${product.id}">Delete</button></td></tr>`).join('') : '<tr><td colspan="5"><div class="empty-state">No products found.</div></td></tr>';
    qsa('[data-edit-product]', root).forEach(button => button.addEventListener('click', () => adminLoadProduct(Number(button.dataset.editProduct))));
    qsa('[data-toggle-visible]', root).forEach(button => button.addEventListener('click', () => { const id = Number(button.dataset.toggleVisible); data.products = data.products.map(product => product.id === id ? { ...product, visible: product.visible === false } : product); adminRenderAll(); }));
    qsa('[data-toggle-stock]', root).forEach(button => button.addEventListener('click', () => { const id = Number(button.dataset.toggleStock); data.products = data.products.map(product => product.id === id ? { ...product, inStock: !(product.inStock !== false) } : product); adminRenderAll(); }));
    qsa('[data-delete-product]', root).forEach(button => button.addEventListener('click', () => { const id = Number(button.dataset.deleteProduct); if (!confirm('Delete this product?')) return; data.products = data.products.filter(product => product.id !== id); adminRenderAll(); }));
    qsa('[data-move-up]', root).forEach(button => button.addEventListener('click', () => moveProduct(Number(button.dataset.moveUp), -1)));
    qsa('[data-move-down]', root).forEach(button => button.addEventListener('click', () => moveProduct(Number(button.dataset.moveDown), 1)));
  }
  function moveProduct(id, direction) { const list = data.products.slice().sort((a, b) => (Number(a.order || 9999) - Number(b.order || 9999)) || (a.id - b.id)); const index = list.findIndex(product => product.id === id); const targetIndex = index + direction; if (index < 0 || targetIndex < 0 || targetIndex >= list.length) return; const firstOrder = Number(list[index].order || index + 1); const secondOrder = Number(list[targetIndex].order || targetIndex + 1); list[index].order = secondOrder; list[targetIndex].order = firstOrder; data.products = list; adminRenderAll(); }
  function adminLoadProduct(id) { const product = (data.products || []).find(item => item.id === id); if (!product) return; qs('#productId').value = product.id; qs('#adminName').value = product.name || ''; qs('#adminCategory').value = product.category || ''; qs('#adminPrice').value = product.price || 0; qs('#adminOldPrice').value = product.oldPrice || 0; qs('#adminBadge').value = product.badge || ''; qs('#adminOrder').value = product.order || ''; qs('#adminDesc').value = product.description || ''; qs('#adminLongDesc').value = product.longDescription || ''; qs('#adminHighlights').value = (product.highlights || []).join(' | '); qs('#adminImage').value = product.image || ''; qs('#adminImages').value = (product.images || []).join('\n'); qs('#adminFeatured').checked = !!product.featured; qs('#adminVisible').checked = product.visible !== false; qs('#adminStock').checked = product.inStock !== false; window.scrollTo({ top: 0, behavior: 'smooth' }); }
  function adminResetProductForm() { const form = qs('#adminProductForm'); if (form) form.reset(); const id = qs('#productId'); if (id) id.value = ''; const visible = qs('#adminVisible'); const stock = qs('#adminStock'); if (visible) visible.checked = true; if (stock) stock.checked = true; }
  function adminRenderCategories() { const root = qs('#adminCategoriesList'); if (!root) return; root.innerHTML = (data.categories || []).map(category => { const count = (data.products || []).filter(product => product.category === category.name).length; return `<div class="list-item"><div><h4>${safeText(category.icon)} ${safeText(category.name)}</h4><p>${safeText(category.copy || '')} • ${count} products</p></div><div class="actions-row"><button type="button" class="btn-mini" data-edit-category="${category.id}">Edit</button><button type="button" class="btn-mini" data-delete-category="${category.id}">Delete</button></div></div>`; }).join(''); qsa('[data-edit-category]', root).forEach(button => button.addEventListener('click', () => adminLoadCategory(Number(button.dataset.editCategory)))); qsa('[data-delete-category]', root).forEach(button => button.addEventListener('click', () => { const id = Number(button.dataset.deleteCategory); const category = data.categories.find(item => item.id === id); if (!category) return; if (data.products.some(product => product.category === category.name)) { setStatus('This category is used by products. Change those products first.', true); return; } data.categories = data.categories.filter(item => item.id !== id); adminRenderAll(); })); }
  function adminLoadCategory(id) { const category = (data.categories || []).find(item => item.id === id); if (!category) return; qs('#categoryId').value = category.id; qs('#categoryName').value = category.name || ''; qs('#categoryIcon').value = category.icon || ''; qs('#categoryCopy').value = category.copy || ''; }
  function adminResetCategoryForm() { const form = qs('#adminCategoryForm'); if (form) form.reset(); const id = qs('#categoryId'); if (id) id.value = ''; }
  function adminFillSettings() { const map = { '#setStoreName': data.settings.storeName || '', '#setShortName': data.settings.shortName || '', '#setWhatsapp': data.settings.whatsappNumber || '', '#setAddress': data.settings.storeAddress || '', '#setPhone1': data.settings.phoneOne || '', '#setPhone2': data.settings.phoneTwo || '', '#setInstagram1': data.settings.instagramOne || '', '#setInstagram2': data.settings.instagramTwo || '', '#setAmazon': data.settings.amazonLink || '', '#setFlipkart': data.settings.flipkartLink || '', '#setMeesho': data.settings.meeshoLink || '', '#setTagline': data.settings.storeTagline || '' }; Object.entries(map).forEach(([selector, value]) => { const el = qs(selector); if (el) el.value = value; }); }
  function adminRenderInquiries() { const root = qs('#adminInquiryList'); if (!root) return; root.innerHTML = (data.inquiries || []).length ? data.inquiries.slice().reverse().map(note => `<div class="list-item"><div><h4>${safeText(note.name || 'Unnamed')} • ${safeText(note.status || 'New')}</h4><p>${safeText(note.phone || 'No phone')} • ${safeText(note.product || 'No product')}</p><p style="margin-top:.35rem;">${safeText(note.note || '')}</p></div><div class="actions-row"><button type="button" class="btn-mini" data-delete-inquiry="${note.id}">Delete</button></div></div>`).join('') : '<div class="empty-state">No inquiry notes saved.</div>'; qsa('[data-delete-inquiry]', root).forEach(button => button.addEventListener('click', () => { const id = Number(button.dataset.deleteInquiry); data.inquiries = data.inquiries.filter(note => note.id !== id); adminRenderInquiries(); })); }
  function adminRenderAll() { adminRenderStats(); adminPopulateCategoryOptions(); adminRenderProducts(); adminRenderCategories(); adminFillSettings(); adminRenderInquiries(); }
  function nextId(list) { return list.length ? Math.max(...list.map(item => Number(item.id || 0))) + 1 : 1; }
  function bindAdmin() {
    const adminPage = qs('body[data-page="admin"]'); if (!adminPage) return; const loginBox = qs('#loginBox'); const adminPanel = qs('#adminPanel'); const loginBtn = qs('#adminLoginBtn'); const logoutBtn = qs('#adminLogoutBtn'); const loginErr = qs('#loginErr');
    function showLoginError(message) { if (!loginErr) return; loginErr.textContent = message; loginErr.classList.remove('hidden'); }
    function clearLoginError() { if (!loginErr) return; loginErr.textContent = ''; loginErr.classList.add('hidden'); }
    if (loginBtn) loginBtn.addEventListener('click', () => { const user = qs('#adminUser')?.value.trim() || ''; const pass = qs('#adminPass')?.value.trim() || ''; if (user === ADMIN_USERNAME && pass === ADMIN_PASSWORD) { clearLoginError(); if (loginBox) loginBox.classList.add('hidden'); if (adminPanel) adminPanel.classList.remove('hidden'); setStatus('Logged in. No admin session is stored after refresh.', false); adminRenderAll(); } else showLoginError('Invalid username or password.'); });
    if (logoutBtn) logoutBtn.addEventListener('click', () => { if (adminPanel) adminPanel.classList.add('hidden'); if (loginBox) loginBox.classList.remove('hidden'); adminResetProductForm(); adminResetCategoryForm(); clearLoginError(); setStatus('', false); const user = qs('#adminUser'); const pass = qs('#adminPass'); if (user) user.value = ''; if (pass) pass.value = ''; });
    const productForm = qs('#adminProductForm'); if (productForm) productForm.addEventListener('submit', async event => { event.preventDefault(); const id = Number(qs('#productId')?.value || 0); const images = (qs('#adminImages')?.value || '').split('\n').map(value => value.trim()).filter(Boolean); const mainImage = (qs('#adminImage')?.value || '').trim() || images[0] || ''; const record = { id: id || nextId(data.products || []), name: qs('#adminName')?.value.trim() || '', category: qs('#adminCategory')?.value || '', price: Number(qs('#adminPrice')?.value || 0), oldPrice: Number(qs('#adminOldPrice')?.value || 0), badge: qs('#adminBadge')?.value.trim() || '', order: Number(qs('#adminOrder')?.value || (data.products.length + 1)), description: qs('#adminDesc')?.value.trim() || '', longDescription: qs('#adminLongDesc')?.value.trim() || '', highlights: (qs('#adminHighlights')?.value || '').split('|').map(value => value.trim()).filter(Boolean), image: mainImage, images: images.length ? images : (mainImage ? [mainImage] : []), featured: !!qs('#adminFeatured')?.checked, visible: !!qs('#adminVisible')?.checked, inStock: !!qs('#adminStock')?.checked }; if (!record.name || !record.category) { setStatus('Product name and category are required.', true); return; } if (id) data.products = data.products.map(product => product.id === id ? record : product); else data.products.push(record); adminResetProductForm(); adminRenderAll(); try { await downloadScriptFile(); setStatus('Product saved. A new script.js file was downloaded. Upload that file to GitHub and refresh the site.', false); showToast('Product saved and script.js downloaded'); } catch (error) { setStatus('Product was updated in memory, but script.js could not be downloaded. Use the Download script.js button.', true); } });
    const productReset = qs('#productResetBtn'); if (productReset) productReset.addEventListener('click', adminResetProductForm);
    const categoryForm = qs('#adminCategoryForm'); if (categoryForm) categoryForm.addEventListener('submit', async event => { event.preventDefault(); const id = Number(qs('#categoryId')?.value || 0); const record = { id: id || nextId(data.categories || []), name: qs('#categoryName')?.value.trim() || '', icon: qs('#categoryIcon')?.value.trim() || '•', copy: qs('#categoryCopy')?.value.trim() || '' }; if (!record.name) { setStatus('Category name is required.', true); return; } if (id) { const old = data.categories.find(category => category.id === id); data.products = data.products.map(product => product.category === old.name ? { ...product, category: record.name } : product); data.categories = data.categories.map(category => category.id === id ? record : category); } else data.categories.push(record); adminResetCategoryForm(); adminRenderAll(); try { await downloadScriptFile(); setStatus('Category saved. A new script.js file was downloaded.', false); showToast('Category saved and script.js downloaded'); } catch (error) { setStatus('Category was updated in memory, but script.js could not be downloaded. Use the Download script.js button.', true); } });
    const categoryReset = qs('#categoryResetBtn'); if (categoryReset) categoryReset.addEventListener('click', adminResetCategoryForm);
    const settingsForm = qs('#adminSettingsForm'); if (settingsForm) settingsForm.addEventListener('submit', async event => { event.preventDefault(); data.settings = { storeName: qs('#setStoreName')?.value.trim() || '', shortName: qs('#setShortName')?.value.trim() || '', whatsappNumber: qs('#setWhatsapp')?.value.trim() || '', storeAddress: qs('#setAddress')?.value.trim() || '', phoneOne: qs('#setPhone1')?.value.trim() || '', phoneTwo: qs('#setPhone2')?.value.trim() || '', instagramOne: qs('#setInstagram1')?.value.trim() || '', instagramTwo: qs('#setInstagram2')?.value.trim() || '', amazonLink: qs('#setAmazon')?.value.trim() || '', flipkartLink: qs('#setFlipkart')?.value.trim() || '', meeshoLink: qs('#setMeesho')?.value.trim() || '', storeTagline: qs('#setTagline')?.value.trim() || '' }; updateSettingsUI(); renderContact(); try { await downloadScriptFile(); setStatus('Settings saved. A new script.js file was downloaded.', false); showToast('Settings saved and script.js downloaded'); } catch (error) { setStatus('Settings were updated in memory, but script.js could not be downloaded. Use the Download script.js button.', true); } });
    const inquiryForm = qs('#adminInquiryForm'); if (inquiryForm) inquiryForm.addEventListener('submit', async event => { event.preventDefault(); data.inquiries.push({ id: nextId(data.inquiries || []), name: qs('#inqName')?.value.trim() || '', phone: qs('#inqPhone')?.value.trim() || '', product: qs('#inqProduct')?.value.trim() || '', status: qs('#inqStatus')?.value || 'New', note: qs('#inqNote')?.value.trim() || '' }); inquiryForm.reset(); adminRenderInquiries(); try { await downloadScriptFile(); setStatus('Inquiry note saved. A new script.js file was downloaded.', false); showToast('Inquiry saved and script.js downloaded'); } catch (error) { setStatus('Inquiry was updated in memory, but script.js could not be downloaded. Use the Download script.js button.', true); } });
    const downloadBtn = qs('#downloadScriptBtn'); if (downloadBtn) downloadBtn.addEventListener('click', async () => { try { await downloadScriptFile(); setStatus('A fresh script.js file was downloaded.', false); showToast('script.js downloaded'); } catch (error) { setStatus('Could not download script.js from this page. Make sure the site is opened from GitHub Pages or a local server.', true); } });
    ['#adminProductSearch', '#adminProductCategoryFilter'].forEach(selector => { const element = qs(selector); if (!element) return; element.addEventListener('input', adminRenderProducts); element.addEventListener('change', adminRenderProducts); });
  }
  function init() { updateNav(); updateSettingsUI(); renderHome(); renderCategories(); renderProducts(); renderDetails(); renderContact(); bindAdmin(); }
  document.addEventListener('DOMContentLoaded', init);
})();
